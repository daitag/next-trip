<h2>投稿一覧ページ</h2>

<%= link_to "+",new_post_path %>

<!-- 一覧表示 -->
<% @posts.each do |post| %>
	<%= attachment_image_tag(post,:image,:fill,200,200) %>
	<%= post.title %>
	<%= link_to "詳細",post_path(post) %>
<% end %>
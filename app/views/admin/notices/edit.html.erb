<h2>編集</h2>

<%= form_for(@notice,url: admin_notice_path) do |f| %>
	<%= f.label :title, "タイトル" %>
	<%= f.text_field :title %>
	<%= f.label :body, "内容" %>
	<%= f.text_area :body %>
	<%= f.attachment_field :image %>
	<%= f.label :status, "公開設定" %>
	<%= f.radio_button :status,true,checked: true %>
	<%= f.label :status,"公開",value: true %>
	<%= f.radio_button :status,false %>
	<%= f.label :status,"非公開",value: false %>
	<%= f.submit "編集" %>
<% end %>